Sources for the plugin `plugin.appodeal.Chartboost`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.Chartboost" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
